(trigger
    (key [[alt][cmd].]))

(name [Close Module Decl])

(only-in source.ml)

(input document)

(output after-selected)

(save nothing)

(script [ruby -- "$TM_SUPPORT_PATH/bin/checknest.rb" '(?x) (functor|structure) (.+) = (?=\s*struct)' 'end' 'end (* $1 $2 *)' -n$TM_LINE_NUMBER])